version: '3.8'

services:
  db:
    image: postgres:16-alpine  # แนะนำใช้รุ่น Alpine (ขนาดไฟล์เล็กกว่า เบาเครื่องกว่า)
    restart: always
    environment:
      POSTGRES_USER: admin           # ตั้งชื่อ User ให้ชัดเจน
      POSTGRES_PASSWORD: admin12345678     # Password
      POSTGRES_DB: fibo_store_db     # ตั้งชื่อ DB ให้ตรงกับโปรเจกต์
    ports:
      - "5432:5432"  # 🚨 สำคัญมาก: ต้องเปิดช่องนี้ ไม่งั้น Python เข้ามาคุยไม่ได้
    volumes:
      - ./pg_data:/var/lib/postgresql/data  # 🚨 สำคัญที่สุด: เซฟข้อมูลลงเครื่องจริง (กันข้อมูลหาย)

  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"